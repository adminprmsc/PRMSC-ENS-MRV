# Copy to backend/.env — only if running NestJS on the host (npm run start:dev).
# If API runs in Docker, use repo .env.docker instead.

NODE_ENV=development
PORT=5001
SECRET_KEY=dev-local-secret-key
JWT_SECRET_KEY=dev-local-jwt-secret-key
DATABASE_URL=postgresql://prmsc:prmsc_local_dev@127.0.0.1:5433/prmsc_mrv?sslmode=disable
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:5174,http://127.0.0.1:5174
